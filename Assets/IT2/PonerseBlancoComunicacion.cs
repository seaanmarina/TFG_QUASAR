using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PonerseBlancoComunicacion : MonoBehaviour
{
    public int poderPonerseBlanco;
    public bool ponerseABlanco;
    // Start is called before the first frame update
    void Start()
    {
        ponerseABlanco = false;
        poderPonerseBlanco = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
